# Stander

一套转发节点与链路的管理系统。控制台分管理端与用户端两个端，边界见 `CLAUDE.md`。

本文件只是术语表：定义每个词**是什么**，不记怎么实现、不记待办、不记决策。

## Language

**套餐**：
一份流量额度与重置周期的组合，是所有管理员共用的目录项，不属于任何一个用户。
一份套餐可以被任意多个用户关联。
_Avoid_: 流量包、订阅、计划、plan（中文语境里）

**关联**：
把一份套餐挂到一个用户身上，同时把该用户的到期时间与流量重置时间一并推到
「当下 + 该套餐的重置周期」。是**覆盖**，不是续费：重新关联不累加剩余时长，
一律从当下重新起算。
_Avoid_: 订阅、绑定、开通、续费

**重置周期**：
套餐的流量额度隔多久归零一次，同时也是关联后的有效期长度。取值是四选一的枚举
（月 / 季 / 半年 / 年），**不是天数**。
_Avoid_: 有效期、周期天数、时长

**额度**：
一个重置周期内允许消耗的流量上限，以字节计。
库里承载它的列是 `traffic_plan.total_traffic`，该列的注释写着「消耗」——**注释是错的**，
存的是上限不是用量。用户实际用掉多少叫**消耗**，那是另一回事，按天记在别处。
_Avoid_: 总流量、流量（单说）；尤其不要跟「消耗」混用
