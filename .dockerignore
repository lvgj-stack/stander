.git
.gitignore
.github
docs
deploy
sql
scripts
*.md
Dockerfile
.dockerignore
# The admin console has its own image and its own build. Without this line the
# Go build context includes web/node_modules — hundreds of megabytes shipped to
# the daemon on every build, for files the binary never uses.
web
