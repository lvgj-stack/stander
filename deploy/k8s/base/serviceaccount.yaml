apiVersion: v1
kind: ServiceAccount
metadata:
  name: stander
# stander talks to MySQL and to agents; it never calls the Kubernetes API, so
# the token is not mounted.
automountServiceAccountToken: false
